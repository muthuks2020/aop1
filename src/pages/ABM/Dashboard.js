import React from 'react';
import SalesRepDashboard from '../SalesRep/Dashboard';

// ABM (Area Business Manager) Dashboard
// Currently uses the same interface as Sales Rep
// Can be customized with additional features like:
// - View commitments from all TBMs in area
// - Area-wide analytics and reports
// - Escalation management

function ABMDashboard() {
  return <SalesRepDashboard />;
}

export default ABMDashboard;
