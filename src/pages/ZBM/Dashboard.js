import React from 'react';
import SalesRepDashboard from '../SalesRep/Dashboard';

// ZBM (Zonal Business Manager) Dashboard
// Currently uses the same interface as Sales Rep
// Can be customized with additional features like:
// - View commitments from all ABMs in zone
// - Zone-wide analytics and reports
// - Strategic planning tools

function ZBMDashboard() {
  return <SalesRepDashboard />;
}

export default ZBMDashboard;
